import { LayerTranslation } from "..";
export declare class LayerKeyMapRepository {
    readonly projectId: string;
    readonly sceneId: string;
    private readonly layerKeyMap;
    constructor(projectId: string, sceneId: string);
    putMap(layerId: string, keyId: string): Promise<void>;
    fetchLayerTranslation(layerId: string): Promise<LayerTranslation | undefined>;
    fetchTranslations(): Promise<ReadonlyArray<LayerTranslation>>;
}
