/// <reference types="node" />
export declare enum ExpireDuration {
    forever = 0,
    day = 1,
    week = 2
}
export interface FileHostingRequest {
    file: Blob | string | Buffer | any;
    name: string;
    private?: boolean;
    expires?: ExpireDuration;
}
export interface FileHostingResult {
    url: string;
    key: string;
    private: boolean;
}
