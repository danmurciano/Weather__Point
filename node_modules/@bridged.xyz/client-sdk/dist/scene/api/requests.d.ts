import { StorableScene } from "../../design";
export interface SceneRegisterRequest extends StorableScene {
    fileId: string;
    projectId: string;
    preview: string;
    linkedComponents?: StorableScene[];
}
export interface LayerRegisterNestedRequst {
}
