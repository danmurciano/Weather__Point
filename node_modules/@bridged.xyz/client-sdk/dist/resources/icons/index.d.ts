import { IconConfig } from "@reflect.bridged.xyz/core";
export declare function loadSvg(key: string, config: IconConfig): Promise<string>;
export declare function makeIconUrl(name: string, config: IconConfig): string;
