import { AxiosInstance } from "axios";
import { CompileDDCResponse, CompileResponse } from "./types";
export declare class DartServices {
    private baseUrl;
    readonly axios: AxiosInstance;
    constructor(baseUrl: string);
    /**
     * cursor that holds retry counts of requests.
     */
    retryCursors: Map<number, number>;
    /**
     * use this for flutter code compile
     * @param source
     */
    compileDDC(source: string, cursor?: number): Promise<CompileDDCResponse>;
    /**
     * use this for pure dart code compile. code with flutter dependency will refuse to compile.
     * @param source
     */
    compile(source: string): Promise<CompileResponse>;
    /**
     * compiles and build decorated js
     * @param source
     */
    compileComplete(source: string): Promise<CompileResponse>;
}
export declare const defaultClient: DartServices;
export declare function compileComplete(source: string): Promise<CompileResponse>;
